# 2026-09-21 debug（开源前）

日期：2026-09-21 16:20  
分支：`v4.0.1-accuracy`

## 命令

| 项 | 结果 |
|---|---|
| `swift test` | 63 通过，0 失败 |
| `dist/v4/diskmon.app` | CFBundle 4.0.1，ad-hoc，pid 27888 已跑约 10 分钟 |
| `dist/kept/4.0.0/` | 仍是 4.0.0，mtime 2026-09-18 21:22 |

## 真机采集

海康威视 C4000 2TB（`disk5`，PCI-Express）：

- `smartctl -a -d nvme`：40 °C，备件 100%，寿命 0%，Critical Warning 0x00，PASSED
- SwiftData `ZTEMPERATURESAMPLE` 最近点：16:21:13 / 16:21:08 / 16:21:03，皆 40.0
- `ZSMARTSNAPSHOT` 最近点：16:21:00，40 / 0 / 100，与 smartctl 一致

USB 3.2 NVMe 盒（RTL9210，10 Gbps）：

- `SMART Status = Not Supported`
- 全部 `smartctl -d`（nvme / sat / sntrealtek / sntasmedia / sntjmicron）失败
- UI 应继续显示「—」，不要写 0 °C

## 崩溃（4.0.0，不是 4.0.1）

`~/Library/Logs/DiagnosticReports/diskmon-2026-09-21-104721.ips`

- 进程：`com.homecenter.diskmon.v4`，2026-09-18 21:22 启动，2026-09-21 10:47 退出
- `EXC_BAD_ACCESS` / `SIGBUS` / `KERN_MEMORY_ERROR`
- 栈顶：SwiftUI `TimelineView` → `NSHostingView.layout`
- 出错地址在 mapped file 的 r-x 段（可执行映射失效）

判断：旧 4.0.0 从外接 ExFAT 卷跑了约 2.5 天，映射的 Mach-O 失效。常见于盒被休眠/重挂、或 `.app` 在运行中被替换。4.0.1 本轮未复现。不要从这份 ips 推断 4.0.1 逻辑崩。

## 存储体积

`~/Library/Application Support/diskmon.store` ≈ 58 MB

- `ZTEMPERATURESAMPLE` 126 383 行（约 5 s 一条，与轮询一致）
- `ZSMARTSNAPSHOT` 78 045 行（最近间隔约 20 s，比笔记里「60 s」更密）

DownSampler 在，但 raw 行仍在涨。开源后若要收，应先验降采样是否真删 raw，而不是再加一层假压缩。

## 刻意没改

- 没为 USB RTL9210 造温度
- 没修 TimelineView（证据只打到 4.0.0 + 外置卷）
- 没把 58 MB store 当场清掉
